# ripcord seed function list — stock_v120 (FNIRSI 2C53T V1.2.0)
#
# RE-ANCHORED 2026-05-30 from stock_v120's OWN bytes (fnirsi-scope-decode
# round 2). Each address below was independently disassembled and confirmed
# to land on a real function prologue / clean instruction-stream start by the
# round-2 adversarial verifier AND the coverage critic. These are the inverse
# of the quarantined cross-source seeds: derived from THIS image, not borrowed.
#
# They sit in the largest genuinely-missed code gap (0x0803AA4E-0x0803D0B8),
# which Ghidra never reached because these are FreeRTOS task bodies / dispatch
# routines entered via pointer or computed branch. Seeding them pulls the
# runtime acquisition engine + its dispatchers into the warehouse with
# decompiled_c — the most FPGA-relevant missed code.
#
# Format: 0xADDR  name  [size]   (size forces full-range disassembly so a body
# gated by an internal jump table is fully decoded, not cut at the table.)
#
# --- Runtime acquisition engine (THE prize) ---
# 7092-byte FreeRTOS task: r7=SPI3_STS(0x40003C08), [r7+4]=SPI3_DT, state base
# sb=0x200000F8, VFP calib (vldr s18). 9-way capture-mode dispatch via a TBH at
# 0x0803B536 keyed on (USART2 cmd byte - 1). Burst -> state+0x5B0/+0x9B0
# (2x512 interleaved CH1/CH2), roll rings state+0x356/+0x483. Size forces the
# 9 TBH-gated mode handlers to decode as part of the body.
0x0803B454  acq_engine_task          7092
# --- Dispatchers reached by pointer/queue (settings, dvom TX, UI/key) ---
0x0803AA50  settings_cmd_dispatcher
0x0803B3F4  dvom_tx_task
0x0803D008  ui_key_dispatch_task
# --- Function Ghidra missed in the 0x0802B71C gap ---
0x0802B71C  usb_disconnect_trigger
#
# HELD (not seeded): 0x08027A50 / 0x0802E664 are re-SEGMENTATION targets (merge
# phantom fragments into 15486/6016-byte bodies) — forcing those risks creating
# fresh mega-functions; revisit deliberately. 0x0802B78C is a non-prologue leaf
# (low value). REJECTED by disasm: 0x0803AB00 (interior of 0x0803AA50),
# 0x0802E8E4 (mid-instruction desync — the IRQ20 label is an artifact).
